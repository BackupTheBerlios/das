dieser ordner enthält die installationsskripten "install.sh", "install.bat" und das file createdb.sql
createdb.sql muss alle sql queries zur erstellung der datenbankstruktur und testdaten enthalten.

install.sh bzw. install.bat wird vom user ausgeführt, um d. datenbank für "DAS" einzurichten.